---
// Astro imports
import { Image } from 'astro:assets';
import fondoGracias from '../../assets/images/fondo-hero.jpg';
import personajeGracias from '../../assets/images/web-hosting2.png';
---

<section class="relative min-h-[70vh] pt-20 flex items-center overflow-hidden">
  <!-- Fondo como imagen absoluta optimizada -->
  <div class="absolute inset-0 -z-10">
    <Image
      src={fondoGracias}
      alt="Fondo institucional para la sección de agradecimiento"
      width={1920}
      height={1080}
      class="w-full h-full object-cover object-center"
      loading="lazy"
    />
  </div>

  <div class="relative max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 flex flex-col md:flex-row items-center justify-between gap-8">
    
    <!-- Texto -->
    <div class="md:w-1/2 text-center md:text-left">
      <h1 class="text-4xl md:text-5xl font-extrabold text-gray-900 drop-shadow-md leading-tight">
        ¡Gracias por contactarnos!
      </h1>
      <p class="mt-4 text-lg text-gray-800 drop-shadow-sm">
        Tu mensaje ha sido enviado correctamente y está en buenas manos. Nuestro equipo revisa cada consulta con atención y te responderá en menos de 24 horas. Tu proyecto digital es tan importante para nosotros como para ti.
      </p>

      <!-- Botón único -->
      <div class="mt-6 flex justify-center md:justify-start">
        <a href="/"
          class="bg-sky-600 text-white font-semibold py-3 px-6 rounded-lg shadow-md hover:bg-sky-700 hover:scale-105 hover:shadow-xl transition duration-300">
          Volver al inicio
        </a>
      </div>
    </div>

    <!-- Imagen del personaje optimizada -->
    <div class="md:w-1/2 flex justify-center">
      <Image
        src={personajeGracias}
        alt="Ilustración institucional de Rock Hosting Plus para la sección de agradecimiento"
        width={400}
        height={400}
        class="max-h-[80vh] w-full max-w-md object-contain"
        loading="lazy"
      />
    </div>
  </div>
</section>